{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter api-server dev\" \"pnpm --filter career-engine dev\"",
    "build": "pnpm --filter api-server build && pnpm --filter career-engine build",
    "start": "pnpm --filter api-server start",
    "typecheck": "tsc --build && pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.1",
    "@tailwindcss/oxide-win32-x64-msvc": "^4.2.2",
    "concurrently": "^9.1.0",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
